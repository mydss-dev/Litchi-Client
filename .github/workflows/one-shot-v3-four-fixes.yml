name: One-shot V3 four fixes
on:
  push:
    branches: [fix/v3-four-followup-regressions-20260920]
    paths: ['.github/workflows/one-shot-v3-four-fixes.yml']
permissions:
  contents: write
jobs:
  apply-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
          lfs: true
      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          channel: stable
          cache: true
      - name: Apply narrowly scoped fixes
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          def replace(path, old, new, expected=1):
              p=Path(path); source=p.read_text(encoding='utf-8'); count=source.count(old)
              if count != expected: raise SystemExit(f'{path}: expected {expected}, found {count}: {old[:100]!r}')
              p.write_text(source.replace(old,new),encoding='utf-8')
          ctl='lib/app/app_controller.dart'
          replace(ctl,'  bool _hasAccountSummary = false;','  bool _hasAccountSummary = false;\n  // Never infer an absent plan from missing/cached account fields.\n  bool _confirmedNoPlan = false;')
          replace(ctl,'  bool get hasAccountSummary => _hasAccountSummary;','  bool get hasAccountSummary => _hasAccountSummary;\n  bool get hasConfirmedNoPlan => _confirmedNoPlan && !hasPlan;')
          replace(ctl,'    final confirmedNoPlan = snap.hasPlan == false;','    // A failed subscription/user request must not imply an empty account.\n    _confirmedNoPlan = snap.hasPlan == false;\n    final confirmedNoPlan = _confirmedNoPlan;')
          replace(ctl,'    _hasAccountSummary = false;','    _hasAccountSummary = false;\n    _confirmedNoPlan = false;',expected=2)
          replace(ctl,'    _authData = authData;\n    await Future.wait([','    _authData = authData;\n    _confirmedNoPlan = false;\n    await Future.wait([',expected=1)
          dash='lib/v3/pages/v3_dashboard_page.dart'
          replace(dash,"import 'package:flutter/material.dart';","import 'package:flutter/material.dart';\nimport 'package:flutter/foundation.dart';")
          replace(dash,"    final confirmedNoPlan = controller.hasAccountSummary &&\n        !controller.isInitialLoading && !controller.hasPlan;","    final confirmedNoPlan = !controller.isInitialLoading &&\n        controller.hasConfirmedNoPlan;")
          replace(dash,'                                  Text(\n                                    _duration(controller.connectedDuration),\n                                    style: TextStyle(\n                                      color: p.inkMuted,\n                                      fontSize: 11,\n                                    ),\n                                  ),','                                  _ConnectionDuration(controller: controller),')
          replace(dash,'class _ModeRail extends StatelessWidget {', '''// Only the small elapsed-time label ticks; the rest of Home stays untouched.
class _ConnectionDuration extends StatefulWidget {
  const _ConnectionDuration({required this.controller});
  final AppController controller;

  @override
  State<_ConnectionDuration> createState() => _ConnectionDurationState();
}

class _ConnectionDurationState extends State<_ConnectionDuration> {
  Timer? _ticker;

  @override
  void initState() {
    super.initState();
    _syncTicker();
  }

  @override
  void didUpdateWidget(covariant _ConnectionDuration oldWidget) {
    super.didUpdateWidget(oldWidget);
    _syncTicker();
  }

  void _syncTicker() {
    if (widget.controller.connectionStatus == ConnectionStatus.connected) {
      _ticker ??= Timer.periodic(const Duration(seconds: 1), (_) {
        if (mounted) setState(() {});
      });
    } else {
      _ticker?.cancel();
      _ticker = null;
    }
  }

  @override
  void dispose() {
    _ticker?.cancel();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) => Text(
    _duration(widget.controller.connectedDuration),
    key: const Key('v3-connection-duration'),
    style: TextStyle(color: V3Palette.of(context).inkMuted, fontSize: 11),
  );
}

/// Show only modes actually supported by this UI platform.
bool v3ShowsNetworkMode(TargetPlatform platform, NetworkMode mode) =>
    mode == NetworkMode.tun ||
    platform == TargetPlatform.windows || platform == TargetPlatform.macOS;

class _ModeRail extends StatelessWidget {''')
          replace(dash,'            for (final mode in NetworkMode.values)\n              Expanded(','            for (final mode in NetworkMode.values)\n              if (v3ShowsNetworkMode(defaultTargetPlatform, mode)) Expanded(',expected=1)
          account='lib/v3/pages/v3_account_page.dart'
          replace(account,'        const SizedBox(height: 16),\n        _PreferencesPanel(controller: controller,\n          busy: _updatingPreferences,\n          onExpireChanged: (v) => _updatePreferences(remindExpire: v),\n          onTrafficChanged: (v) => _updatePreferences(remindTraffic: v),\n          onAutoRenewalChanged: (v) => _updatePreferences(autoRenewal: v)),','        if (controller.hasPlan) ...[\n          const SizedBox(height: 16),\n          _PreferencesPanel(controller: controller,\n            busy: _updatingPreferences,\n            onExpireChanged: (v) => _updatePreferences(remindExpire: v),\n            onTrafficChanged: (v) => _updatePreferences(remindTraffic: v),\n            onAutoRenewalChanged: (v) => _updatePreferences(autoRenewal: v)),\n        ],')
          traffic='lib/v3/pages/v3_traffic_page.dart'
          replace(traffic,'if (!controller.hasPlan && controller.hasAccountSummary) {','if (controller.hasConfirmedNoPlan && !controller.isInitialLoading) {')
          test=Path('test/v3_four_followup_fixes_test.dart')
          test.write_text('''import 'package:flutter/foundation.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:litchi_client/shared/models/app_models.dart';
import 'package:litchi_client/v3/pages/v3_dashboard_page.dart';

void main() {
  test('network mode display matches Windows macOS Android Linux', () {
    for (final platform in [TargetPlatform.windows, TargetPlatform.macOS]) {
      expect(v3ShowsNetworkMode(platform, NetworkMode.system), isTrue);
      expect(v3ShowsNetworkMode(platform, NetworkMode.tun), isTrue);
    }
    for (final platform in [TargetPlatform.android, TargetPlatform.linux]) {
      expect(v3ShowsNetworkMode(platform, NetworkMode.system), isFalse);
      expect(v3ShowsNetworkMode(platform, NetworkMode.tun), isTrue);
    }
  });
}
''',encoding='utf-8')
          PY
          dart format lib/app/app_controller.dart lib/v3/pages/v3_dashboard_page.dart lib/v3/pages/v3_account_page.dart lib/v3/pages/v3_traffic_page.dart test/v3_four_followup_fixes_test.dart
      - run: flutter pub get
      - name: Targeted tests
        run: flutter test test/v3_four_followup_fixes_test.dart
      - name: Full test suite
        run: flutter test
      - name: Static analysis
        run: flutter analyze
      - name: Commit fixes and remove temporary workflow
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/one-shot-v3-four-fixes.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/app/app_controller.dart lib/v3/pages/v3_dashboard_page.dart lib/v3/pages/v3_account_page.dart lib/v3/pages/v3_traffic_page.dart test/v3_four_followup_fixes_test.dart .github/workflows/one-shot-v3-four-fixes.yml
          git commit -m 'fix(v3): restore elapsed timer and gate plan/platform UI correctly'
          git push origin HEAD:fix/v3-four-followup-regressions-20260920
