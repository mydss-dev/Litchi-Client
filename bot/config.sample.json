{
  // ═══════════════════════════════════════════════════════════════════════════
  //  Litchi 客户端远程配置文件
  // ═══════════════════════════════════════════════════════════════════════════
  //
  //  说明：此 JSON 对象即签名后交付给客户端的配置 payload。
  //        填写完成后请将整段内容复制发送给 Bot，Bot 会自动：
  //          1. 剥离注释
  //          2. 校验字段格式
  //          3. Ed25519 签名
  //          4. 生成可托管的 signed config
  //
  //  必填字段：app_name、api_base_list、logo_url
  //  建议填写：invite_url_base、avatar_url、update_download_url
  //  可选留空：update_version、update_changelog
  // ═══════════════════════════════════════════════════════════════════════════

  // ── 应用名称（必填，1-40 字符）─────────────────────────────────────────────
  "app_name": "My VPN",

  // ── API 服务器地址列表（必填，至少一个 https:// URL）───────────────────────
  //  客户端按顺序尝试，首个可连通的地址会被持续使用。
  "api_base_list": [
    "https://api.example.com",
    "https://api2.example.com"
  ],

  // ── API 路径前缀（可选，以 / 开头，不超过 80 字符）─────────────────────────
  //  大多数面板填 "/api/v1"，留空表示无前缀。
  "api_prefix": "/api/v1",

  // ── Logo 图片 URL（必填，https://）─────────────────────────────────────────
  "logo_url": "https://example.com/logo.png",

  // ── 头像图片 URL（可选）────────────────────────────────────────────────────
  "avatar_url": "https://example.com/avatar.png",

  // ── 邀请链接基础 URL（建议填写，https://）──────────────────────────────────
  //  用于自动补全注册/邀请链接。
  "invite_url_base": "https://example.com",

  // ── 强制更新版本号（可选，如 "1.3.0"）───────────────────────────────────────
  //  客户端版本低于此值时提示强制更新。留空不启用。
  "update_version": "",

  // ── 各平台下载地址（可选）──────────────────────────────────────────────────
  //  支持两种格式：
  //    1. 单一 URL 字符串（所有平台共用）
  //    2. 按平台分别指定：{"windows":"...","macos":"...","android":"..."}
  //  留空 {} 不启用更新下载。
  "update_download_url": {
    "windows": "https://example.com/windows-setup.exe",
    "macos": "https://example.com/macos.dmg",
    "android": "https://example.com/android.apk"
  },

  // ── 更新日志（可选，不超过 200 字符）───────────────────────────────────────
  "update_changelog": "修复已知问题，优化用户体验。"
}
