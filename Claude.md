你现在扮演一名高级软件工程师、技术负责人、架构审计专家，请基于当前项目进行一次完整的代码审计与工程化优化评估。

这次任务不是简单找 bug，也不是只看单个文件，而是要从整个项目的工程质量出发，系统性检查项目结构、目录规范、命名规范、架构设计、代码质量、可维护性、可扩展性、性能、安全性、API 封装、状态管理、类型定义、异常处理、配置管理、构建流程等方面。

你的目标是帮助这个项目逐步达到接近大厂工程化标准的状态。

请注意：本轮只做审计、分析和优化方案，不要直接修改代码。等我确认方案后，再按优先级分阶段执行重构。

请重点检查以下内容：

一、项目结构

请检查当前项目目录是否合理，包括但不限于：

- 目录层级是否清晰
- 页面、组件、业务模块、API、状态管理、工具函数、类型定义、常量、配置是否分层明确
- 是否存在文件放错位置的问题
- 是否存在业务逻辑、UI、请求逻辑混在一起的问题
- 是否存在目录命名不统一的问题
- 是否需要重新规划目录结构

请基于当前项目实际情况，给出推荐的项目结构，不要泛泛而谈。

二、命名规范

请检查当前项目中的命名是否统一，包括但不限于：

- 目录命名
- 文件命名
- 组件命名
- 函数命名
- 变量命名
- API 文件命名
- hooks 命名
- store 命名
- 类型定义命名
- 常量命名
- 配置文件命名

请指出不合理、不清晰、不统一、语义不准确的命名，并给出推荐替换方案。

三、代码质量

请检查当前代码是否存在以下问题：

- 重复代码
- 过长函数
- 过大组件
- 职责不清晰
- 过度耦合
- 魔法字符串
- 魔法数字
- 无意义封装
- 过度封装
- 可读性差
- 维护成本高
- 技术债堆积

请指出具体位置、具体问题，并给出可落地的优化方式。

四、架构设计

请从架构角度检查当前项目是否适合长期维护和继续扩展，包括但不限于：

- UI 层、业务逻辑层、数据请求层是否解耦
- 页面是否承担了过多业务逻辑
- API 请求是否统一封装
- 状态管理是否清晰
- 类型定义是否集中且可复用
- 配置是否集中管理
- 是否适合后续新增功能
- 是否存在牵一发动全身的问题

请判断当前架构处于什么阶段，例如：原型阶段、可上线阶段、工程化待完善阶段、需要重构阶段等。

五、API 与数据流

请检查当前 API 对接和数据流设计，包括但不限于：

- API 请求是否统一封装
- 是否有统一的 request client
- 是否有统一的错误处理
- 是否有统一的响应结构处理
- loading、empty、error 状态是否完整
- 接口返回类型是否清晰
- 是否存在页面直接调用底层请求的问题
- 是否存在前端过度依赖后端字段的问题
- 是否存在重复请求、无效请求、过度请求的问题

请给出推荐的数据流和 API 分层方案。

六、状态管理

请检查当前状态管理是否合理，包括但不限于：

- 哪些状态应该是本地状态
- 哪些状态应该是全局状态
- 哪些状态应该来自缓存或服务端状态
- 是否存在状态重复存储
- 是否存在状态更新混乱
- 是否存在状态命名不清晰
- 是否存在组件之间通过不合理方式传递状态的问题

请给出优化建议。

七、类型定义

如果项目使用 TypeScript，请重点检查：

- 类型定义是否完整
- 是否存在 any 滥用
- 是否存在重复类型
- 是否存在接口返回值没有类型约束
- 是否存在组件 props 类型不清晰
- 是否存在 API 类型和业务类型混在一起
- 类型文件是否应该统一整理

请给出类型定义的组织方式和命名建议。

八、安全性与稳定性

请检查项目是否存在以下风险：

- 敏感信息暴露
- token 处理不合理
- 环境变量使用不规范
- 接口异常未处理
- 空值异常风险
- 权限判断不严谨
- 错误提示不清晰
- 用户输入未校验
- 配置写死
- 日志输出敏感信息

请指出风险等级和修复建议。

九、性能与体验

请检查是否存在以下问题：

- 不必要的重复渲染
- 不必要的重复请求
- 大组件导致维护和渲染成本过高
- 静态资源加载不合理
- 路由是否可以懒加载
- 组件是否可以按需加载
- 数据是否可以缓存
- 是否有明显影响客户端体验的问题

请给出具体优化建议。

十、工程化规范

请检查项目工程化是否完善，包括但不限于：

- ESLint 是否合理
- Prettier 是否统一
- TypeScript 配置是否严格
- Git 提交规范是否需要补充
- 环境变量是否分环境管理
- 构建配置是否清晰
- 是否需要增加路径别名
- 是否需要补充 README
- 是否需要补充开发规范文档
- 是否需要补充错误边界、日志、监控等基础能力

请给出建议。

最终请按以下格式输出审计报告：

# 项目代码审计与工程化优化报告

## 一、整体评价

请从技术负责人视角评价当前项目整体工程质量，判断当前项目属于以下哪种状态：

- 原型阶段
- 可继续开发但需要规范化
- 可上线但工程化不足
- 需要局部重构
- 需要系统性重构

请说明判断依据。

## 二、核心问题总结

请用表格列出核心问题：

| 优先级 | 问题类型 | 问题描述 | 影响 | 建议处理方式 |
| ------ | -------- | -------- | ---- | ------------ |

优先级分为：

- P0：必须立即修复
- P1：强烈建议优化
- P2：可以逐步优化

## 三、项目结构审计

请说明当前目录结构的问题，并给出推荐的新目录结构。

推荐目录结构请使用树状结构输出，例如：

src/
app/
pages/
routes/
features/
components/
services/
stores/
hooks/
utils/
types/
constants/
config/
assets/

并说明每个目录应该放什么、不应该放什么。

## 四、命名规范审计

请指出当前项目中不规范的命名，并给出推荐替换方案。

请使用表格：

| 当前命名 | 问题 | 推荐命名 | 原因 |
| -------- | ---- | -------- | ---- |

## 五、模块拆分建议

请指出哪些文件、页面、组件或模块过大，应该如何拆分。

请使用表格：

| 文件/模块 | 当前问题 | 建议拆分方式 | 拆分后收益 |
| --------- | -------- | ------------ | ---------- |

## 六、代码质量问题

请列出具体代码质量问题，包括重复代码、复杂逻辑、可读性问题、类型问题、异常处理问题等。

每个问题请包含：

- 问题位置
- 当前问题
- 为什么这是问题
- 推荐优化方式
- 优化后的收益

## 七、API 与数据流优化建议

请说明当前 API 和数据流存在的问题，并给出推荐方案。

请重点说明：

- API service 如何组织
- request client 如何封装
- 错误处理如何统一
- 类型如何绑定 API
- 页面如何调用业务 service
- loading、empty、error 状态如何处理

## 八、状态管理优化建议

请说明当前状态管理是否合理，并给出建议：

- 哪些状态保留在组件内部
- 哪些状态放到 store
- 哪些状态应该作为 server state 处理
- 是否需要缓存
- 是否需要拆分 store

## 九、类型定义优化建议

如果项目使用 TypeScript，请说明：

- 类型文件如何组织
- API 类型如何命名
- 业务模型如何命名
- 组件 props 如何命名
- 哪些 any 应该替换
- 哪些重复类型应该合并

## 十、安全性与稳定性建议

请列出安全和稳定性风险，并按严重程度排序。

请使用表格：

| 风险等级 | 风险点 | 影响 | 修复建议 |
| -------- | ------ | ---- | -------- |

## 十一、性能优化建议

请列出可以优化性能和体验的地方。

请说明：

- 是否需要懒加载
- 是否存在重复请求
- 是否存在重复渲染
- 是否需要缓存
- 是否需要拆分大组件
- 是否需要优化资源加载

## 十二、工程化建设建议

请从大厂项目规范角度，给出工程化建议：

- ESLint
- Prettier
- TypeScript strict
- Git commit 规范
- 路径别名
- 环境变量管理
- README
- 开发规范文档
- 错误边界
- 日志系统
- 构建流程
- CI/CD

## 十三、重构优先级路线图

请给出一个安全、分阶段、可执行的重构路线图。

格式如下：

### 阶段 1：低风险规范化

目标：
执行内容：
风险：
收益：

### 阶段 2：目录结构和模块边界优化

目标：
执行内容：
风险：
收益：

### 阶段 3：API、类型、状态管理统一

目标：
执行内容：
风险：
收益：

### 阶段 4：性能、安全、体验优化

目标：
执行内容：
风险：
收益：

## 十四、下一步建议

请用项目负责人视角告诉我：

- 当前项目是否适合继续堆功能
- 哪些地方必须先整理
- 哪些地方可以边开发边优化
- 是否存在未来维护风险
- 下一步最推荐我让 AI 执行的具体任务是什么

最后请给出一个明确结论：

“建议先做审计确认，再分阶段重构，不建议一次性大规模改动。”

要求：

- 不要客套。
- 不要泛泛而谈。
- 必须基于当前项目实际代码。
- 所有建议必须可执行。
- 不要直接修改代码。
- 不要为了显得专业而过度设计。
- 不要引入项目当前不需要的复杂架构。
- 优先保证稳定性、可维护性和后续可扩展性。
