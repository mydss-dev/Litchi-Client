我来直接克隆并审计这个仓库的真实代码。先把项目拉下来看结构。审视可执行文件发现机制与错误处理流程审视可执行文件发现机制与错误处理流程已经发现关键信息：核心是用 Go 源码（cgo/FFI）方式编译进客户端的，且 runtime/geo/ 只有 country.mmdb 和 geosite.dat，没有 geoip.dat。让我深入读核心生命周期代码。剖析控制器自动端口切换机制与连接路径。剖析控制器自动端口切换机制与连接路径。核心管理写得相当完整。关键观察：mixed-port 失败检测靠日志字符串匹配（脆弱），且 \_waitForPortFree 只检查 API 端口不检查 7890。让我读编排层 core_controller.dart。确认端口自动切换和系统代理指向机制正常运作。确认端口自动切换和系统代理指向机制正常运作。编排层很完善：节点切换/模式切换后都会 \_closeConnections()，TUN 模式有 TunInterfaceVerifier 主动验证，端口也有 LocalPortAllocator.choose 做切换。但系统模式没有对 mixed-port 的主动探活。让我读端口分配、代理设置、配置生成等关键文件。分析代理快照机制与原始设置恢复逻辑。分析代理快照机制与原始设置恢复逻辑。LocalPortAllocator 确认了 7890 占用会自动切换到临时端口（Q10/Q11 YES）。TunInterfaceVerifier 对 TUN 做了主动验证（Q28 好）。但 macOS TUN 没有 Kill Switch（代码注释自认）。现在读最关键的代理恢复逻辑和配置生成。评估配置安全性与端口绑定策略。评估配置安全性与端口绑定策略。ProxySetter 很扎实：用 proxy_snapshot.json 保存并恢复原始系统代理（不是简单关闭），有所有权校验避免覆盖用户后改的设置，启动时 disableIfStale 做崩溃恢复。Q12-16 desktop 处理得好。现在读最关键的配置生成。剖析Android核心与VpnService的TUN集成机制剖析Android核心与VpnService的TUN集成机制配置生成质量很高：allow-lan: false、bind-address: 127.0.0.1、external-controller 绑定 loopback + 随机 secret（Random.secure），TUN 的 strict-route/auto-route/dns-hijack/fake-ip 都合理，不支持的节点类型会被过滤。
但发现两个隐患：(1) 当订阅提供 rules 时，硬编码的私网 DIRECT 规则和 MATCH 兜底会被整体丢弃；(2) ipv6: false 但 TUN 不接管 v6，宿主有 v6 时可能泄漏。让我读核心 Go 源、Android 原生、macOS 提权和打包脚本。剖析Android TUN流量拦截机制与配置参数。剖析Android TUN流量拦截机制与配置参数。核心是 metacubex/mihomo v1.19.27（真实的现代 Meta 版本）。Android 用 cgo/FFI 嵌入式库（VpnService 提供 TUN fd），desktop 用独立 mihomo 二进制（Process.start）。让我看 main.go 的 TUN/错误回传，以及 Android 原生层和打包脚本。审视Android GEO资源打包与加载机制差异审视Android GEO资源打包与加载机制差异There's GeoAssets.kt for Android geo handling and full VpnService implementation. Let me read the Android native layer and the manifest.验证Android TUN流量拦截机制实现细节验证Android TUN流量拦截机制实现细节关键发现：Android 的 GeoAssets.kt 也只复制 country.mmdb + geosite.dat（无 geoip.dat — 这其实合理，默认 GEOIP 用 mmdb）。但 find 显示 android/app/src/main/assets/ 目录下没有这些文件——如果 CI 不在构建时注入，Android 核心会因为找不到 GEO 而启动失败。我必须查 CI。先读 VPN 服务和引擎。核心引擎验证与DNS配置同步中核心引擎验证与DNS配置同步中Android VpnService 实现到位：addRoute("0.0.0.0",0) + addRoute("::",0) 全量接管 v4/v6（Q23 真实接管），underlyingNetworks 回调处理网络切换，onRevoke 处理系统撤销，停止时正确释放 fd。现在读引擎、权限申请流程（Q21）和 Android 管理器。分析VPN权限流程与通知权限申请逻辑。分析VPN权限流程与通知权限申请逻辑。Engine 处理了 fd 双关闭、native lib 加载失败回退、TUN DNS 同步。现在读 VPN 权限申请流程（MainActivity）和 Dart 端管理器，以及 CI 构建脚本（关键：.so 和 GEO assets 是否真的打进包）。验证VPN权限流程与核心启动状态准确性。验证VPN权限流程与核心启动状态准确性。VPN 权限流程清晰：prepareVpn → 先要通知权限 → VpnService.prepare，拒绝时 onActivityResult 返回 false。现在读 Dart 端 Android 管理器和构建脚本（验证 .so 与 GEO 是否真的进包）。审视打包脚本验证GEO文件和二进制文件。审视打包脚本验证GEO文件和二进制文件。Q21 确认处理得好：consent 拒绝 → 明确中文提示「需要允许 VPN 权限后才能连接」。Q22 状态用 core/vpn 双层标志 + 原生事件维护，准确。
现在查最关键的构建/打包：CI 是否真的把 .so、GEO 文件、desktop mihomo 二进制打进包。审视了多平台构建脚本与打包流程的完整性。审视了多平台构建脚本与打包流程的完整性。有专门的打包脚本。让我读 Windows/macOS 打包、Android .so 构建、gradle 配置和 CI 主流程，确认 GEO + 二进制 + .so 真的进包。审视了三端打包流程、GEO文件验证、Android JNI库命名和资源打包的关键问题。审视了三端打包流程、GEO文件验证、Android JNI库命名和资源打包的关键问题。关键发现：打包脚本完整（Windows/macOS 下载并 SHA256 校验 mihomo 二进制 + wintun + GEO；Android 用 NDK 构建 cgo .so）。
但注意：Go 构建产物是 liblitchi_mihomo.so，而引擎 System.loadLibrary("litchi_core") 找的是 liblitchi_core.so——需要确认 CMake JNI 包装层。还要确认 Android assets 里的 GEO 从哪来（build_mihomo_android.sh 没复制）。读 CI、gradle、JNI 层、版本锁和文件大小。确认 Gradle 任务构建流程与地理数据资源配置。确认 Gradle 任务构建流程与地理数据资源配置。非常清晰了：GEO 文件是 Git LFS 指针（132 字节，真实文件 8.25MB），core_versions.env 的 SHA256 锁与 LFS oid 完全匹配——构建时 SHA 校验，没 git lfs pull 会构建失败而非发布损坏文件（防御正确）。Android gradle 有专门 task 把 runtime/geo 复制进 assets。两层 .so 设计（Go liblitchi_mihomo.so + CMake JNI liblitchi_core.so）解释了命名。
读 gradle GEO task、JNI 层、CI 顺序、macOS 提权、API 客户端，以及 rules 来源。验证Android构建依赖和Windows跨平台编译配置。验证Android构建依赖和Windows跨平台编译配置。Android 打包链路严谨：ensureMihomoLibraries（构建 .so，release 缺失则失败）+ ensureGeoAssets（复制 GEO 进 assets，SHA256 校验，原子提升，LFS 没拉则失败）。需确认这些 task 真的挂到了构建依赖上。读 CI、依赖挂载、macOS 提权、API 客户端、rules 来源。分析 macOS 特权 TUN 启动机制与守护进程设计。分析 macOS 特权 TUN 启动机制与守护进程设计。打包链路全部确认：preBuild 强依赖两个 task，CI 三端都 lfs: true，Windows/macOS 打包脚本输出路径与 findExecutable 候选完全对应。Q3/Q4/Q26 三端打包/加载扎实。
读最后几块：macOS 提权 TUN、流量统计准确性、rules 来源（验证 MATCH 兜底隐患）。验证订阅解析器规则匹配和私有IP直连保留情况。验证订阅解析器规则匹配和私有IP直连保留情况。macOS 提权 TUN 设计巧妙：osascript 一次性授权 + root 看门狗（app 退出或 .stop 文件即杀核心，防 root 孤儿进程），ps 只读检测避免二次授权。但确认 macOS TUN 无 Kill Switch（崩溃窗口可能泄漏，代码自认）。流量统计读 /traffic 的 up/down（准确），延迟测试用 group delay（可用），所有 API 调用带 Bearer secret。
最后确认 rules 来源（MATCH 兜底隐患）和连接请求构建。验证订阅解析器规则处理和私有IP直连保留逻辑。验证订阅解析器规则处理和私有IP直连保留逻辑。rules 来自订阅/面板（V2Board 风格）。让我确认 subscription_parser.dart 是否保证 MATCH 兜底存在、是否保留私网 DIRECT 规则——这决定我那条 P2 的严重性。验证诊断日志有效性与隐私保护机制。验证诊断日志有效性与隐私保护机制。rules 直接取自订阅 YAML（仅规范化策略名），既不追加 MATCH 兜底、也不前置私网 DIRECT。当订阅规则集本身规范（V2Board 默认模板通常以 MATCH 结尾且含 GEOIP,private/CN,DIRECT）时没问题，但缺失时会"已连接但部分站点失败"。确认为条件性 P2。
最后查日志文件（Q51）和 UI 连接指示绑定。验证UI状态机准确反映连接错误状态验证UI状态机准确反映连接错误状态日志体系完善：diagnostic.log（256KB 轮转、token/密码/UUID/订阅URL/服务器地址全部脱敏）+ crash.log（未捕获异常）。脱敏很彻底（利于隐私，但会降低对具体节点故障的诊断价值）。
让我确认连接 UI 指示绑定，并尝试在沙箱跑静态分析/测试。审视单元测试覆盖范围与安全加固实现细节。审视单元测试覆盖范围与安全加固实现细节。UI 直接绑定 ConnectionStatus（含 error 状态显示"重连"），无独立的"伪已连接"路径——唯一的已连接指示就是 status == connected，而控制器只在真实校验后才置位。
沙箱无 Flutter/Go/NDK 工具链（且 Linux 容器本就无法运行 Windows GUI / Android VPN），所以我做的是对真实克隆代码的静态审计。最后看一眼测试覆盖作为质量信号。审视代码成熟度、梳理三端连接链路、识别P0/P1/P2风险。审视代码成熟度、梳理三端连接链路、识别P0/P1/P2风险。工具链确认不可用（flutter/dart/go 均未安装，且 Linux 容器本就无法运行 Windows GUI 或 Android VpnService），所以这是一次对真实克隆代码的静态源码审计，不是运行时黑盒测试。代码库有 143 个单元测试（含 security_hardening_test、mihomo_config_test、tun_interface_verifier_test、macos_privileged_core_test 等），成熟度较高，但没有真正拉起核心验证代理连通性的 E2E 测试——我标记的运行时风险（mixed-port 竞态、TUN 泄漏窗口）正是测试盲区。
下面是完整审计。

Litchi-Client 三端可用性 + 连接链路审计报告
审计对象：github.com/mydss-dev/Litchi-Client @ 4452b10
核心：metacubex/mihomo v1.19.27（真实现代 Meta 版本）
审计方式：静态源码审计（已克隆全仓库逐文件阅读核心链路；沙箱无 Flutter/Go/NDK 工具链，无法运行时验证）
架构：桌面端用独立 mihomo 二进制（Process.start）；Android 用 cgo/FFI 嵌入式 .so（VpnService 提供 TUN fd）

一、总体结论
结论：核心连接链路质量明显高于同类开源客户端，工程化扎实，适合上线（建议作为正式版或公测）。 未发现会导致"三端无法启动 / 系统代理回不来 / 配置不安全"的 P0 级上线阻断问题。
支撑判断的关键事实：

三端打包/核心定位是可信的。Windows/macOS 通过 SHA256 校验下载官方 mihomo 二进制 + wintun，并把 country.mmdb/geosite.dat 从 runtime/geo（Git LFS）拷贝到 exe 同级 / Contents/Resources；Android 用 preBuild.dependsOn(ensureMihomoLibraries, ensureGeoAssets) 强制构建 .so 并把 GEO 复制进 assets，全部带 SHA256 校验，LFS 没拉会构建失败而非发布损坏包。CoreManager.findExecutable() 的候选路径与三端打包输出路径精确对应。
系统代理会"快照-恢复"原始设置（proxy_setter.dart 的 proxy_snapshot.json），不是简单 ProxyEnable=0；有所有权校验避免覆盖用户后改的代理；启动时 disableIfStale() 做崩溃恢复。这是很多客户端做错的地方，这里做对了。
Mihomo 配置默认安全：allow-lan: false、bind-address: 127.0.0.1、external-controller 绑 loopback + Random.secure() 生成的随机 secret，所有 API 调用带 Bearer。
状态机基本诚实：UI 的"已连接"唯一来源是 ConnectionStatus.connected，而控制器只在真实校验后置位（系统模式有 mixed-port 绑定失败检测、TUN 模式有 TunInterfaceVerifier 主动验证网卡、Android 有 VPN running 事件）。有独立的 error 状态并在 UI 显示"重连"。

但有一类必须在上线前/后尽快收敛的 P1 风险：系统代理模式下"核心起来了但 mixed-port 实际没起来"的兜底，完全依赖对 mihomo 日志字符串的匹配，且与 API 就绪存在竞态，缺少对监听端口的主动探活——这是"显示已连接但实际不通"的唯一现实来源。另有 macOS TUN 无 Kill Switch、桌面 TUN 的 IPv6 旁路、订阅规则缺 MATCH 兜底等 P2 加固项。

二、问题清单（P0/P1/P2/P3）

格式：影响平台 · 触发条件 · 代码位置 · 风险 · 修复

P0（上线阻断）
无。 未发现导致三端无法启动、系统代理无法恢复、配置不安全或必然性"假已连接"的问题。

P1（强烈建议上线前修，或上线后第一个补丁）
P1-1　mixed-port 失败检测靠日志字符串 + 与 API 就绪竞态，且无主动探活 →"已连接但不通"的现实来源

平台：Windows / macOS（系统代理模式）
触发条件：

选定代理端口在 LocalPortAllocator.choose() 选中后、mihomo 真正 bind 前被别的进程抢占（TOCTOU 窗口）；
mihomo 因其它原因没能 bind mixed-port，但 external-controller 正常；
关键竞态：mihomo 的 mixed bind 失败日志在 \_waitForApi() 返回 true 之后才被 Dart 的 stream 处理，此时 proxyPortBindFailed 仍是 false，于是判定为 running。

代码位置：lib/shared/services/core_manager.dart:152-167（handleOutput 里 lower.contains('mixed') && contains('server error') && contains('bind')）、:219 \_waitForApi 之后的 :234 检查；以及 lib/app/core_controller.dart:352-373 的"核心已运行→只切代理"快路径（完全不重新校验 mixed-port）。
风险：系统代理被指向一个没在监听的端口 → 浏览器全部超时，但 UI 显示"已连接"、计时器在走。这是最容易被用户感知为"这软件是假的"的故障。isReady()（mihomo_api_client.dart:30）只校验 /proxies 返回 map，不代表 mixed-port 在监听。
修复方向（具体）：在系统模式 \_core.isRunning 之后、ProxySetter.enable 之前，对 127.0.0.1:\_activeProxyPort 做主动探活：

dart // core*controller.dart：连接成功判定前插入
Future<bool> \_proxyPortAlive(int port) async {
try {
final s = await Socket.connect('127.0.0.1', port,
timeout: const Duration(milliseconds: 800));
s.destroy();
return true; // 端口在监听
} catch (*) { return false; }
}
系统模式（含 :352 快路径与 :446 全路径）都改为：探活失败 → \_coreError = '代理端口未监听' + error 状态 + 停核。更彻底的做法是发一个走 127.0.0.1:port 的 HTTP CONNECT/请求实测能否代理（类似桌面版做 connectivity check）。同时给 core_manager.dart 的日志匹配加宽（mihomo 实际可能输出 inbound listen error/address already in use/bind: 等多种措辞），降低对单一字符串的依赖。
P1-2　核心日志失败检测是单字符串脆弱匹配，mihomo 升级即可能静默失效

平台：Windows / macOS
触发条件：升级 bundled mihomo（已是 v1.19.27，后续版本日志措辞变化）后，mixed+server error+bind 三词不再同现。
代码位置：core_manager.dart:157-161。
风险：P1-1 的兜底彻底失效，回归到纯 API 判活 → 假已连接概率上升。
修复：以 P1-1 的主动探活为主判据，日志匹配只作辅助；并对失败措辞做多模式集合匹配。

P2（加固项，建议排期）
P2-1　macOS TUN 无 Kill Switch，核心崩溃存在流量泄漏窗口

平台：macOS（TUN 模式）
触发条件：TUN 已连，mihomo 核心异常退出（非用户主动断开）。
代码位置：core_controller.dart:826-830（注释自认 "macOS TUN currently has no firewall-level kill switch"，仅 ProxySetter.disable()——但 TUN 模式本就没设系统代理，等于 no-op）；Windows 侧有 WindowsTunKillSwitch（WFP）做对照。
风险：utun 网卡随核心消失后，路由回落到物理网卡的瞬间，流量可能明文泄漏。Windows 有 WFP 兜底，macOS 没有。
修复：macOS 用 pf（packet filter）实现 Kill Switch：连接成功后写入只放行 mihomo 出站 + loopback 的 pf 锚规则，核心退出/异常时由那个 root 看门狗（macos_privileged_core.dart 的 watchdog 脚本，已是 root）顺手 pfctl -e -f 启用阻断锚，断开时 pfctl 卸载。看门狗已具备 root 权限，扩展成本低。

P2-2　桌面 TUN 下 IPv6 旁路/泄漏

平台：Windows / macOS（TUN 模式，宿主有可用 IPv6 时）
触发条件：mihomo_config.dart:102 ipv6: false 且 desktop tun 块（:146-156）未声明 inet6 地址/不接管 v6；宿主有原生 IPv6。
代码位置：mihomo_config.dart:99-156。（注：Android 在 LitchiVpnService.openTun() 显式 addRoute("::",0) 接管 v6，无此问题，仅桌面。）
风险：v6 流量绕过 TUN 走物理网卡 → 真实 IP 泄漏 / 分流失效。
修复：二选一——(a) 端到端开启 IPv6（需确认节点支持）；(b) 保持 ipv6:false 但在 TUN 模式让网卡接管 v6 并在核心内 REJECT，或在 auto-route 下补 v6 黑洞路由，避免明文外泄。

P2-3　订阅提供 rules 时，丢弃私网 DIRECT 兜底与 MATCH 兜底

平台：三端
触发条件：订阅/面板下发了 rules（生产常态），且该规则集本身缺少终结 MATCH/FINAL 或缺少私网段 DIRECT。
代码位置：mihomo_config.dart:67,175——safeRules 非空时整体替换 fallbackRules，硬编码的私网 DIRECT 块（:69-77）和 MATCH,PROXY（:78）被丢弃；subscription_parser.dart:131-138 只规范化策略名，不补 MATCH、不留私网 DIRECT。
风险：若服务端规则集不规范 → 局域网/内网被代理或 fake-ip，或无兜底导致未匹配流量被 REJECT →"已连接但部分站点打不开"。V2Board 默认模板通常 OK，但不可假定第三方面板都规范。
修复（具体）：在 buildFullConfig 里对 safeRules 做两件事——

dart // 1) 始终前置私网 DIRECT（首条命中，重复无害）
final merged = [...privateDirectRules, ...safeRules];
// 2) 若无终结 MATCH/FINAL 则补一条
final hasMatch = merged.any((r) {
final t = r.split(',').first.trim().toUpperCase();
return t == 'MATCH' || t == 'FINAL';
});
final rulesOut = hasMatch ? merged : [...merged, 'MATCH,$selectorTag'];
P2-4　Android 前台服务 START_NOT_STICKY，内存压力被杀后 VPN 不自恢复

平台：Android
触发条件：系统在低内存下杀掉进程（前台服务被杀概率低但非零）。
代码位置：LitchiVpnService.kt:81,89,96,109,115 全部 return START_NOT_STICKY。
风险：VPN 静默掉线且不重连，用户以为还在连接（但 isVpnRunning 静态量随进程死亡复位，重开 App 会显示断开，所以不会假已连接，只是体验上"莫名断了"）。
修复：评估对持久连接场景改 START_STICKY 并在 onStartCommand(null intent) 时按上次配置自恢复；或维持现状但在通知里提示。属产品取舍。

P3（轻微 / 一致性 / 体验）

P3-1　\_connectedAt 在重启后重置 →连接时长不准：core*controller.dart:111（Android init 时 isVpnRunning 为真则置 DateTime.now()），重开 App 后 VPN 仍连但时长从 0 算。桌面端因 cleanupOnStartup 会杀残留核心、重连为新会话，属预期。修复：持久化 connectedAt。
P3-2　geoip.dat 在 \_geoFiles/\_copyRuntimeAssets 列表里但从未打包：core_manager.dart:70、macos_privileged_core.dart:249。无害（默认 GEOIP 用 country.mmdb），但是误导性死代码。建议删掉或加注释说明 geodata-mode 才需要。
P3-3　Windows 未设 ProxyOverride 旁路表：proxy_setter.dart:\_winEnable 只写 ProxyServer+ProxyEnable，本地/内网地址也走代理（靠 mihomo 规则兜底）。建议补 localhost;127.*;10._;172.16._-172.31.\_;192.168.\*;<local>。
P3-4　VPN 授权结果在 Activity 重建时可能丢失：MainActivity.kt:168 的 vpnPermissionResult 若在系统授权弹窗期间 Activity 被销毁重建，回调丢失 → Dart prepareVpn() future 永挂。低概率。建议持久化 pending 状态或加超时。
P3-5　UI 设置仍显示 7890 而实际端口已切换：LocalPortAllocator 切到临时端口后仅 SecureLogger.debug（core_controller.dart:383）记录，用户不可见。建议把实际监听端口回显到 UI。
P3-6　日志脱敏过度，降低节点级排障价值：secure_logger.dart 把 server/server_name/dial endpoint 也 [REDACTED]，diagnostic.log 里看不出是哪个节点失败。隐私与可诊断性的取舍，可考虑对本地诊断日志保留服务器主机名（不含凭证）。
P3-7　startCoreOnly 桌面预载失败是静默的：core_controller.dart:223 只 SecureLogger.debug，不置 error。可接受（用户显式点连接时会暴露），但首屏延迟测试可能无声失败。

三、逐项 Checklist 对照（按你给的 50+ 点）
启动与核心定位（1-5）

三端启动：✅ 路径/打包匹配，错误分支完备。
三端起核心：✅ 桌面 Process.start(mihomo)，Android System.loadLibrary 两层 .so。
核心文件定位：✅ findExecutable() 候选覆盖 exe 同级 / core/ / Contents/Resources / LOCALAPPDATA|Application Support；找不到→'未找到 mihomo 核心，请重新安装客户端'。
GEO 打包加载：✅ country.mmdb+geosite.dat 三端均打包并复制到工作目录，SHA256 校验；geoip.dat 未打包但默认不需要（GEOIP 走 mmdb）。
失败 UI 提示：✅ 明确中文（core_error_message_service.dart，端口占用/缺核心/权限/TUN/配置错误均有专属文案 + l10n）。

连接与状态准确性（6-12）

点连接状态准确：✅ ConnectionStatus 五态 + UI 绑定，TUN 有网卡验证。
"假已连接"：⚠️ P1-1——系统模式缺 mixed-port 主动探活，存在窄竞态。
external-controller 正常但 mixed-port 失败识别：⚠️ P1-1/P1-2——有日志检测但脆弱+竞态。
7890 占用自动切换：✅ LocalPortAllocator.choose(preferred:7890) 占用则切临时端口。
系统代理指向实际端口：✅ ProxySetter.enable(port:\_activeProxyPort) 用实际选中端口。
断开关代理/VPN/TUN：✅ 断开/shutdown/stopAndReset/异常退出均 ProxySetter.disable()，TUN 额外 \_core.stop()+释放 WFP。

Windows/macOS 系统代理（13-19）

系统代理模式：✅ Windows 注册表+WinInet 广播；macOS networksetup 逐服务。
mixed-port 监听：⚠️ 见 P1-1。
设到 127.0.0.1:端口：✅。
异常退出恢复原代理：✅ 快照恢复 + disableIfStale（恢复原始值非简单关闭）。
节点切换立即生效：✅ switchNode→API switchProxy→_closeConnections()（强制旧连接重建）。
延迟测试：✅ /group/PROXY/delay 批量 + 单节点 delay，gstatic 204。
时长/上下行：✅ /traffic 流读 up/down Bps；⚠️ 时长重启后重置（P3-1）。

Android VPN/TUN（20-26）

VPN 权限申请：✅ prepareVpn→VpnService.prepare，拒绝→'需要允许 VPN 权限后才能连接...'。
startCoreOnly/startVpn/stopVpn/stopCore 状态：✅ core/vpn 双层标志 + 原生事件维护，区分"核心就绪≠已连接"。
TUN 真正接管：✅ addRoute("0.0.0.0",0)+addRoute("::",0) 全量 v4/v6，fd 交 sing-tun。
断开释放 VPN：✅ stopVpnLayer：注销回调、native stopVpn、stopForeground(REMOVE)、fd 单方所有权防双关闭。
后台/切页/重启恢复：✅ init() 读 isVpnRunning+controllerPort/secret 恢复；onRevoke 处理系统撤销；⚠️ 内存杀进程不自恢复（P2-4）、时长重置（P3-1）。
GEO assets 复制/读取：✅ gradle ensureGeoAssets 打进 APK，GeoAssets.stage() 运行时复制到 filesDir。

TUN 模式（27-33）

Windows/macOS 启用：✅ Windows wintun + 配置 tun；macOS osascript 提权起 root 核心。
失败回退/提示：✅ TunInterfaceVerifier.waitUntilReady 网卡没出现→'TUN 虚拟网卡启动失败,请以管理员身份运行或重试'+停核；macOS 取消授权→'已取消管理员授权,TUN 未启动'。
TUN DNS：✅ dns-hijack:[any:53]+fake-ip+default-nameserver+proxy-server-nameserver，Android 额外同步底层网络 DNS。
strict-route/auto-route/dns-hijack：✅ 合理（strict+auto 组合防泄漏，桌面）。
TUN 异常退出泄漏：⚠️ Windows WFP 兜底；macOS 无 Kill Switch（P2-1）；桌面 IPv6 旁路（P2-2）。
Kill Switch 覆盖：⚠️ Windows ✅（\_onCoreStateChanged 保活 WFP）；macOS ❌（P2-1）。

规则/全局/直连（34-40）

规则分流：✅ rules/GEOIP/GEOSITE 透传（country.mmdb/geosite.dat 在位）。
全局：✅ setMode('global')+把 GLOBAL 组指向 PROXY 选择器。
直连：✅ mode:direct。
切换调 API：✅ MihomoApiClient.setMode（PATCH /configs）。
切换关旧连接：✅ setMode→_closeConnections()（避免旧连接续用旧规则）。
MATCH 兜底：✅ 默认 fallback 有 MATCH,PROXY；⚠️ 订阅供 rules 时不补 MATCH（P2-3）。

Mihomo 配置生成（41-47）

mixed-port/external-controller/secret/allow-lan/bind-address：✅ 全部安全（loopback+随机 secret+allow-lan:false+bind 127.0.0.1）。
proxy-groups：✅ PROXY(select)+AUTO(url-test) 生成正确。
selected 应用：✅ 启动后 switchProxy(selectedTag)。
url-test/AUTO：✅ gstatic 204，interval 600，tolerance 50，lazy。
字段兼容：✅ \_normalizedToMihomo 覆盖 tls/reality/ws/grpc/h2 等映射，\_repairProxyReferences 修悬空 dialer-proxy。
过滤不支持类型：✅ 不在 \_supportedTypes 的 raw outbound 返回 null 丢弃；URI 解析失败丢弃。

日志与错误（48-52）

日志定位核心失败：✅ 核心 stdout/stderr 进 500 行内存环 + \_lastError 暴露到 UI；⚠️ 核心原始日志未持久化到独立文件（仅 SecureLogger.warn 进 diagnostic.log）。
端口/GEO缺失/配置/TUN 权限中文提示：✅ 均有明确中文。
diagnostic.log/crash.log：✅ 都有（256KB 轮转 + 未捕获异常 crash.log，凭证脱敏）；⚠️ 脱敏过度降低节点级排障（P3-6）。
误导性"已连接"：⚠️ 仅 P1-1 的窄竞态，无独立伪连接路径。

四、重点测试用例（预期结果 + 命中代码）
用例预期代码依据风险点Windows 7890 被占用自动切临时端口，系统代理指向该端口，正常连LocalPortAllocator.choose + ProxySetter.enable(\_activeProxyPort)UI 仍显示 7890（P3-5）Windows 系统代理 连/断连：注册表写入+WinInet 广播；断：恢复快照原值\_winEnable/\_winRestoreSnapshotIfOwned✅macOS 系统代理 连/断逐网络服务 set web/secure proxy；断恢复每服务原状\_macSetProxy/\_macRestoreSnapshotIfOwnednetworksetup 可能需授权，无专属提示Android VPN 权限拒绝不起 VPN，提示"需要允许 VPN 权限..."AndroidCoreManager.startVpn prepareVpn 分支Activity 重建丢回调（P3-4）Android VPN 正常连接core-only→prepare→startVpn→running，全量 v4/v6 接管\_toggleAndroidConnection + openTun✅TUN 模式连接起核心→TunInterfaceVerifier 等网卡→（Win）engage WFP→connectedcore_controller.dart:417-453macOS 无 Kill Switch（P2-1）、桌面 v6 旁路（P2-2）规则/全局/直连切换API setMode 成功后 \_closeConnections()setMode✅节点切换API switchProxy 成功后 \_closeConnections()switchNode✅GEO 文件缺失构建期 SHA 校验失败→构建中断；运行期 best-effort 跳过（核心可能回落下载，防火墙后失败）ensureGeoAssets/\_ensureGeoAssets运行期缺失无显式 UI 提示，依赖核心启动超时文案Mihomo 核心不存在findExecutable()→null→"未找到 mihomo 核心，请重新安装客户端"core_manager.dart:113-115✅核心启动但 mixed-port 失败应识别并报错，实际存在竞态可能漏判→假已连接core_manager.dart:152-167,234P1-1（本审计最高优先级）

五、具体代码修复方向（按优先级，可直接落地）

1. （P1）系统模式加端口主动探活——在 core_controller.dart 系统模式两处置 connected 前（:362 快路径、:446-447 全路径）插入对 127.0.0.1:\_activeProxyPort 的 Socket.connect 探活（代码见 P1-1）。这是把"假已连接"从"依赖日志"升级为"实测端口/实测代理"的根本修法，改动局部、收益最大。
2. （P1）拓宽核心失败日志匹配——core_manager.dart:157-161 改为对一组失败措辞做集合匹配（bind、address already in use、listen、inbound、mixed…任意命中即标记），并把主动探活作为主判据、日志作辅助。
3. （P2-3）配置补 MATCH + 私网 DIRECT——mihomo_config.dart:175 处对 safeRules 前置私网 DIRECT 块、缺 MATCH 则补 MATCH,$selectorTag（代码见 P2-3）。一段纯逻辑，无副作用。
4. （P2-1）macOS pf Kill Switch——复用 macos_privileged_core.dart 已有的 root 看门狗：连接成功时下发 pf 锚（只放行 mihomo uid 出站 + loopback），核心异常时 pfctl 启用阻断、断开时卸载。与 Windows WFP 对齐。
5. （P2-2）桌面 TUN IPv6——mihomo_config.dart 的桌面 tun 块按"开 v6 端到端"或"接管 v6 并 REJECT/黑洞"二选一处理，消除明文旁路。
6. （P3 批量） 持久化 connectedAt（修时长）、删 geoip.dat 死代码、补 Windows ProxyOverride、UI 回显实际端口。
